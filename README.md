gdg_demo
========

GDG Dakar | Demo apps

You need to have pip installed. Get it with : sudo apt-get install python-pip
One you have it, just tpye : sudo pip install -r requirements.txt

Launch Flask server with:  python main.py